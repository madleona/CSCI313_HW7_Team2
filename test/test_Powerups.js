import Powerups from "..prefabs/Powerups.js";

describe("Powerups", function () {
    let assert = chai.assert;

    it("Can be created", function () {
        let model = new Powerups();
        assert.isOk(true);
    });
});